spring.application.name=skillmentor

# Active sub configuration profile to run (dev/prod)
# ===
# Prod
# ---
#spring.profiles.active=${PROFILE}

# Local
# ---
spring.profiles.active=prod

# Port to run the app on
# ===
# Render injects PORT at runtime; fallback to 8081 for local dev
server.port=${PORT:8081}

# Database Configuration
# ===
# Database Driver config
spring.datasource.driver-class-name=org.postgresql.Driver
# JPA/Hibernate Configuration
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Prod
# ---
#spring.datasource.url=${DATABASE_URL:jdbc:postgresql://db.zzaxyilahgcqllvlyuux.supabase.co:5432/postgres}
#spring.datasource.username=${DB_USERNAME:postgres}
#spring.datasource.password=${DB_PASSWORD:En8xdyw5ebSY8Tw0}

# Local
# ---
spring.datasource.url=jdbc:postgresql://localhost:5432/skill_mentor_v3
spring.datasource.username=postgres
spring.datasource.password=admin

# Local with Docker
# ---
#spring.datasource.url=jdbc:postgresql://host.docker.internal:5432/skill_mentor_v2
#spring.datasource.username=root
#spring.datasource.password=root

# Redis Configuration (Disabled for now)
# ===
#spring.data.redis.host=${REDIS_HOST}
#spring.data.redis.port=${REDIS_PORT}
#spring.data.redis.password=${REDIS_PASSWORD}
#spring.data.redis.database=0
#spring.data.redis.ssl.enabled=true
#spring.data.redis.timeout=60000ms

# Cache Configuration (Disabled for now)
#app.cache.enabled=${CACHE_ENABLED:true}
#spring.cache.type=redis
#spring.cache.redis.time-to-live=600000
#spring.cache.redis.key-prefix=skillmentor:cache:
#spring.cache.redis.use-key-prefix=true
